/*
 * 二叉树路径总和
*/
#include <stdio.h>
#include "Tree.h"
#include <vector>
#include <stack>

using namespace std;

vector<vector<int> > res;
vector<int> path;

void FindAllPath(Tree* root, int sum)
{
  // if (!root) {
  //   return ;
  // }
  // if (root->left == NULL && root->right == NULL && sum == 0) {
  //   res.push_back(path);
  //   return ;
  // }
  // path.push_back(root->value);
  // FindAllPath(root->left, sum - root->value);
  
}
int main()
{
  return 0;
}

